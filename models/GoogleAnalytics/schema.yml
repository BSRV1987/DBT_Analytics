
version: 2

models:
    - name: GA_Sessions
      description: "GoogleAnalytics Sessions"
      columns:
          - name: FULLVISITORID
            description: "The primary key for this table"
    - name: GA_Totals_Agg
      description: "GoogleAnalytics Totals"
      columns:
          - name: Date
            description: "The primary key for this table"
    - name: GA_TotalHits_Agg
      description: "GoogleAnalytics total entraces and exits"
      columns:
          - name: Date
            description: "The primary key for this table"
    
    
